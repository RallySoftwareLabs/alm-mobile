{{#unless onLoginScreen}}
<div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
  <div class="row">
    <div class="col-xs-2 left-button">{{{ this.left_button }}}</div>
    <div class="col-xs-8 title"><div class="ellipsis">{{ this.title }}</div></div>
    <div class="col-xs-2 right-button">{{{ this.right_button }}}</div>
  </div>
</div>
{{/unless}}